name: polyumi_image

image_shape: &image_shape [3, 256, 256]
shape_meta: &shape_meta
  # acceptable types: rgb, low_dim
  obs:
    image:
      shape: *image_shape
      type: rgb
    agent_pos:                  # gripper width only — absolute pose is excluded
      shape: [1]
      type: low_dim
  action:
    shape: [8]

env_runner:
  _target_: diffusion_policy.env_runner.noop_image_runner.NoopImageRunner

dataset:
  _target_: diffusion_policy.dataset.polyumi_image_dataset.PolyUMIImageDataset
  zarr_path: /media/dexnex_ssd/polyumi/replay_buffers/scene_2026-05-26_17-25-43_d044.zarr
  horizon: ${horizon}
  pad_before: 0
  pad_after: 0
  seed: 42
  val_ratio: 0.02
  max_train_episodes: null
  cache_to_gpu: cuda:0    # set to e.g. "cuda:0" to keep the buffer on GPU; needs num_workers=0
